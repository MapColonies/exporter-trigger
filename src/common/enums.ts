//todo - should be replaced and imported from exporter SDK
enum ArtifactType {
  METADATA = 'METADATA',
  GPKG = 'GPKG',
}

export enum TileFormatStrategy {
  FIXED = 'fixed',
  MIXED = 'mixed',
}

export { ArtifactType };
